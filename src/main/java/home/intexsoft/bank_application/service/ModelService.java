package home.intexsoft.bank_application.service;

import home.intexsoft.bank_application.dao.*;
import org.hibernate.SessionFactory;
import org.hibernate.cfg.Configuration;

public abstract class ModelService {

//    private SessionFactory sessionFactory = new Configuration().configure().buildSessionFactory();
//    ClientDAO clientDAO = new ClientDAO(sessionFactory);
//    BankDAO bankDAO = new BankDAO(sessionFactory);
//    CurrencyDAO currencyDAO = new CurrencyDAO(sessionFactory);
//    BankClientDAO bankClientDAO = new BankClientDAO(sessionFactory);
//    BankAccountDAO bankAccount = new BankAccountDAO(sessionFactory);

}
